# switch
> 开关组件

## type值
`switch`

## 默认值
false

## 配置

支持element-UI定义的所有配置。

### 代码示例

```javascript
{
  key: 'switch_switch1',
  title: '开关',
  type: 'switch',
  onInput(value, item, model) {
    console.log('switch onInput', value)
  }
}
```

